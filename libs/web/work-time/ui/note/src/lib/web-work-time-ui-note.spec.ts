import { webWorkTimeUiNote } from './web-work-time-ui-note';

describe('webWorkTimeUiNote', () => {
  it('should work', () => {
    expect(webWorkTimeUiNote()).toEqual('web-work-time-ui-note');
  });
});
